import React from 'react';
import './AppreciationMessage.css'; // Add CSS for styling if needed

const AppreciationMessage = () => {
    return (
        <div className="appreciation">
            
        </div>
    );
};

export default AppreciationMessage;